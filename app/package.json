{
  "name": "pin-app",
  "version": "1.0.0",
  "private": true,
  "description": "Aplicacion base para PIN Proyecto 1",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage --test-coverage-lines=80 --test-coverage-functions=80 --test-coverage-branches=50",
    "lint": "eslint .",
    "build": "echo 'Build OK'"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "express": "^5.2.1",
    "prom-client": "15.1.3",
    "redis": "^6.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.1",
    "supertest": "^7.2.2"
  }
}
